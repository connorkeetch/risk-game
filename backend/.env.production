# Production Environment - Railway Live Database
NODE_ENV=production
PORT=${PORT:-5001}

# Frontend URL - Production domain
FRONTEND_URL=https://conquestk.com

# Database Configuration - Railway PostgreSQL for production
DB_TYPE=postgresql
DATABASE_URL=${DATABASE_URL}

# JWT Configuration - Strong production secret
JWT_SECRET=${JWT_SECRET}

# Redis Configuration (Railway Redis for production)
REDIS_URL=${REDIS_URL}

# Logging - Minimal logging for production
LOG_LEVEL=warn

# CORS Configuration - Production domain
CORS_ORIGIN=https://conquestk.com

# Railway specific configurations
RAILWAY_ENVIRONMENT=production

# Security settings for production
SECURE_COOKIES=true
TRUST_PROXY=true